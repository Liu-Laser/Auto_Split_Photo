# Pull Request: 优化auto_split技能 - 解决首次识别照片数量不足的问题

## 描述

根据用户反馈，发现auto_split技能在首次使用时（特别是清除上下文后）经常识别出较少的照片。此次优化解决了这个问题，提高了扫描照片分割的成功率和效率。

## 主要修改

### 1. 修改脚本默认参数

**scripts/auto_split.py**:
- 将默认 `--method` 从 `"edge"` 改为 `"adaptive"`
- 将默认 `--threshold` 从 `0.5` 降低到 `0.3`

### 2. 更新文档

**SKILL.md**:
- 更新了默认参数说明
- 说明了adaptive方法的真正工作原理
- 添加了推荐的使用示例
- 更新了故障排除建议

## 问题解决

### 原问题
- 同一张扫描图片，第一次运行只识别出1张照片
- 第二次运行（使用adaptive方法）能正确识别出4张照片

### 解决方案
1. **adaptive方法优势**：自动尝试Otsu、Edge、Color三种方法，选择得分最高的
2. **更高灵敏度**：降低threshold到0.3，更容易捕捉照片边界
3. **智能评分**：根据照片数量、面积分布等因素综合评分

### 验证结果
修改后首次运行就能正确识别出4张照片：
```
Otsu: 5 raw -> 4 valid (score=24.5)
Edge: 3 raw -> 2 valid (score=22.3)
Color: 7 raw -> 4 valid (score=25.8)
Selected: color (4 photos)
```

## 适用场景

此次优化特别适合：
- 相册扫描（一张扫描图包含多张老照片）
- 证件扫描（身份证/护照正反面）
- 集体照裁剪（大合影中单人照片）
- 票据拆分（多张收据/发票拼在同一页）

## 测试用例

使用文件：`扫描_20260820.jpg`（5100x7013像素）
- **修改前**：只识别出1张照片
- **修改后**：正确识别出4张照片

## 影响范围

- **向后兼容**：所有现有参数仍然可用
- **性能提升**：默认情况下更准确
- **用户体验**：减少手动调整参数的需要

---

**注意**：由于网络连接问题，此PR暂无法推送到GitHub。请稍后在网络恢复后推送，或使用以下命令：

```bash
git push origin fix-over-cropping-final
```
或
```bash
git push --set-upstream origin fix-over-cropping-final
```